# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(spatialTaskLib)
ADD_LIBRARY(${SPATIAL_TASKLIB_NAME} SHARED 
    ${SPATIAL_TASKLIB_SOURCES})
add_dependencies(${SPATIAL_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${SPATIAL_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${SPATIAL_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${SPATIAL_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${SPATIAL_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${SPATIAL_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${SPATIAL_TASKLIB_HEADERS}
    DESTINATION include/orocos/spatial)

